function square(num) {
    return num * num;
}

const square = (num) => {   //si la logica del programa es mas compleja debemos agregar el return
    return num * num;
}

const square = num => num * num;    //return implicito, si solo tenemos un parametro podemos omitir el parentesis